import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crzbld.rsgquiz',
  appName: 'RSG Quiz',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
